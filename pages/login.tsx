import { Typography } from "@mui/material";
import type { NextPage } from "next";

const Login: NextPage = () => {
  return <Typography variant="h2">Greetings from Login</Typography>;
};

export default Login;
